# Server
PORT=4000
NODE_ENV=development

# Auth
JWT_SECRET=change_this_to_a_long_random_secret_in_production
JWT_EXPIRES_IN=12h

# Database (SQLite file - free, no external DB server needed)
DB_PATH=./data/atomix.db

# CORS - the URL your frontend (Vite) runs on
CLIENT_ORIGIN=http://localhost:3000
